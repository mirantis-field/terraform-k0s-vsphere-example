instance-id: ${hostname}
local_hostname: ${hostname}
network:
  version: 2
  ethernets:
    ens160:
      dhcp4: false #true to use dhcp
      addresses: 
        - ${ip_addr}/24
      gateway4: ${gateway_addr} 
      nameservers:
        addresses: [8.8.8.8]
